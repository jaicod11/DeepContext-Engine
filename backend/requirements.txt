# ──────────────────────────────────────────────────────────────
# requirements.txt
# Pin all versions for reproducible production builds.
# Run: pip install -r requirements.txt --break-system-packages
# ──────────────────────────────────────────────────────────────

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9       # file uploads

# Config & validation
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv==1.0.1

# LangChain core + integrations
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-google-genai>=1.0.0  # Gemini LLM + embeddings
langchain-pinecone>=0.2.3      # Pinecone vector store

# Pinecone client
pinecone-client>=4.0.0

# LLM providers
google-genai                   # Gemini SDK (new, replaces google-generativeai)
ollama                          # Ollama Python client

# Reranker
sentence-transformers==3.0.1   # cross-encoder/ms-marco-*
torch==2.3.0                   # CPU build; swap for cu121 on GPU servers

# Embeddings
numpy==1.26.4

# Document loaders
pypdf==4.2.0                   # PDF parsing
python-docx==1.1.0
python-pptx==1.0.2          # PPTX extraction
openpyxl==3.1.2             # XLSX extraction
unstructured==0.14.5           # General-purpose loader

# Text splitting
langchain-text-splitters        # RecursiveCharacterTextSplitter
tiktoken==0.7.0                 # token-aware chunking

# Caching & rate limiting
redis==5.0.4
hiredis==2.3.2                 # faster Redis protocol parser

# Object storage
boto3==1.34.99                 # AWS S3
google-cloud-storage==2.17.0   # GCP GCS

# Observability
structlog==24.1.0
prometheus-fastapi-instrumentator==6.1.0
sentry-sdk[fastapi]==2.3.1

# HTTP client (used by embeddings REST calls + internal calls)
httpx==0.27.0

# Utilities
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
aiofiles==23.2.1

# Dev / testing (not installed in production Docker image)
pytest==8.2.0
pytest-asyncio==0.23.6
coverage==7.5.0
ruff==0.4.4
mypy==1.10.0